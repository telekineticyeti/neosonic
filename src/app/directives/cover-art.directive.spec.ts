import { CoverArtDirective } from './cover-art.directive';

describe('CoverArtDirective', () => {
  it('should create an instance', () => {
    const directive = new CoverArtDirective();
    expect(directive).toBeTruthy();
  });
});
